# jamelao/raiz

> A base do projeto

# Executando

Para executar a "main" do projeto, que fica em `raiz/cli.py`, execute:

```shell 
uv run -m raiz
```

# Testando

Para executar todos os testes do projeto

```shell 
uv run -m unittest
```
